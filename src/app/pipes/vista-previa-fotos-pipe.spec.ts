import { VistaPreviaFotosPipe } from './vista-previa-fotos-pipe';

describe('VistaPreviaFotosPipe', () => {
  it('create an instance', () => {
    const pipe = new VistaPreviaFotosPipe();
    expect(pipe).toBeTruthy();
  });
});
